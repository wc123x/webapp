#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_htmlhint@0.9.13@htmlhint/bin/htmlhint" "$@"
  ret=$?
else 
  node  "$basedir/../../../_htmlhint@0.9.13@htmlhint/bin/htmlhint" "$@"
  ret=$?
fi
exit $ret
